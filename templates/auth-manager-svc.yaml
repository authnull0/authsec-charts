apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-auth-manager
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-auth-manager
  ports:
    - protocol: TCP
      port: {{ .Values.authmanager.port }}
      targetPort: {{ .Values.authmanager.port }}
  type: ClusterIP
