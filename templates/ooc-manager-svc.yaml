apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ooc-manager
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-ooc-manager
  ports:
    - protocol: TCP
      port: {{ .Values.oocmanager.port }}
      targetPort: {{ .Values.oocmanager.port }}
  type: ClusterIP
